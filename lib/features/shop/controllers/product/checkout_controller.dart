import 'package:flutter/material.dart';

class CheckoutController extends StatelessWidget {
  const CheckoutController({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
